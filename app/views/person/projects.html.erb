<h2>All Projects of <%= @firstname+" "+@lastname %></h2>
<% if @projects["results"].empty? %>
  <p>No Projects found</p>
<% else %>
  <table class="styled-table">
    <thead>
    <tr>
      <th>Projects</th>
      <th>Details</th>
    </tr>
    </thead>
    <tbody>
    <% @projects["results"].each do |result| %>
      <tr>
        <td><%= result["title"].html_safe %></td>
        <td>
          <%= link_to projects_detail_path(result["id"]), class: 'searchResponseIconsLink' do %>
            <%= image_tag 'detail.png', class: "btn-img" %>
          <% end %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
<% end %>